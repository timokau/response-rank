name: bt_meanpref
version: 1

defaults:
  - bt
  - override dataset: multipref_tietotal
  - override dataset/train_annotation_aggregator: random_choice
  - _self_

dataset:
  train_annotation_aggregator:
    bt_target: mean_preference
