name: rr_consensus
version: 2

defaults:
  - rr
  - override train_processor: consensus
  - override stratifier: global_split_ties
  - override train_partitioner: round_robin_auto
  - override dataset: multipref_tietotal
  - _self_
